apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kept out of the base because clusters differ in how the GPU reaches the scheduler: k3s needs
# this DaemonSet applied, while minikube ships it as the `nvidia-device-plugin` addon.
resources:
  - 30-nvidia-device-plugin.yaml
